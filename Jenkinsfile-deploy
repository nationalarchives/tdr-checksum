@Library("tdr-jenkinslib@add-release-to-github-step") _

deployToLambda(
        stage: params.STAGE,
        libraryName: "checksum",
        version: params.TO_DEPLOY,
        deploymentPackageName: "checksum.jar"
)
